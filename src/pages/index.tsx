import Hero from '@components/Hero';
import SocialMedia from '@components/SocialMedia';

const Home = () => {
  return (
    <div>
      <main>
        <SocialMedia />
        <Hero />
      </main>
    </div>
  );
};

export default Home;
