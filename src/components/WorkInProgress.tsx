import EmptyPage from '@components/EmptyPage';

const TITLE = 'Work In Progress';
const DESCRIPTION =
  "Penguin is working hard to make this page release happen soon. For now I'm the only maintainer of this project, but in the future you'll be able to help me";

const WorkInProgress = () => {
  return <EmptyPage title={TITLE} description={DESCRIPTION} />;
};

export default WorkInProgress;
